# TypeScript Config

Contains base TypeScript settings used by root `tsconfig.json`.

- Main file: `tsconfig.base.json`
- Defines shared compiler options (strictness, path alias).
- Root `tsconfig.json` adds include/exclude project scope.
