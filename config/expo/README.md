# Expo Config

Contains the source Expo app config used by the root `app.config.js` entry file.

- Main file: `app.config.js`
- Defines app metadata, icons, splash, bundle/package identifiers, plugins, and EAS extras.
