# Firebase Config Assets

Houses Firebase rule/index files referenced by root `firebase.json`.

- `firestore.rules` - Firestore security rules.
- `firestore.indexes.json` - Firestore composite/single-field indexes.

Note:
- Root `firebase.json` remains at project root for Firebase CLI compatibility.
