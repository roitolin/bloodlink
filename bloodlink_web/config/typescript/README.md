# TypeScript Config (Web)

Contains web TypeScript project configs:

- `tsconfig.root.json` - parent references file.
- `tsconfig.app.json` - app/browser config.
- `tsconfig.node.json` - node-side config (Vite config typing).
